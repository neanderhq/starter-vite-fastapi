from alembic import op
import sqlalchemy as sa

revision = "0001_todos"
down_revision = None
branch_labels = None
depends_on = None


def upgrade():
    op.create_table("todos", sa.Column("id", sa.Uuid(), primary_key=True, server_default=sa.text("gen_random_uuid()")), sa.Column("title", sa.String(200), nullable=False), sa.Column("done", sa.Boolean(), nullable=False, server_default=sa.text("false")))


def downgrade():
    raise RuntimeError("Automatic destructive migrations are not supported")
